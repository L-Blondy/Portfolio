{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "entry.js",
  "scripts": {
    "start": "rm -rf dist && parcel index.html",
    "build": "rm -rf dist && parcel build index.html 404.html --public-url https://l-blondy.github.io/Portfolio/",
    "deploy": "npm run build && copyfiles robots.txt dist && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^2.2.0"
  },
  "dependencies": {
    "lb-lazy-images": "^2.0.7",
    "lb-burger": "^1.1.0",
    "lb-onvisible": "^1.0.9",
    "lb-write": "^1.0.6",
    "sass": "^1.24.3"
  }
}